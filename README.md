# Naresh_SMS_EmailService

Clone the repository into your local

#Steps to run

1) cmd-> npm install (to install required node modules)<br>
2) cmd-> node server.js (to run email service)<br>
3) cmd-> node app.js (to run sms service)<br>
